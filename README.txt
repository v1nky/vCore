HideGryphons
HideBags
HideBinds
HideTrackingBar
HideBindsBars1and2
HideNamesRaidFrames

CleanActionBar

ExtraButtons

ScaleCastBar
ScaleFocusFrame

RarePortraits


CleanActionBar

    HideGryphons
    HideUpDown
    HideArtwork



    -- hold alt to buy stack
    vCore:RegisterDefaultSetting("AltBuy", true)
    -- Double right click to survey at digsites.
    vCore:RegisterDefaultSetting("ArchaeologyHelper", true)
    -- Automaticly role greed on green items at max level.
    vCore:RegisterDefaultSetting("AutoGreed", true)
    -- Hold shift to automaticly accept and turnin quests.
    vCore:RegisterDefaultSetting("AutoQuest", true)
    -- Adds clothed and naked buttons to the dressroom frame.
    vCore:RegisterDefaultSetting("Dressroom", true)
    -- Adds clothed and naked buttons to the dressroom frame.
    vCore:RegisterDefaultSetting("Durability", true)
    -- Adds durability information to the character frame.
    vCore:RegisterDefaultSetting("ErrorFilter", true)
    -- Filter common error spam.
    vCore:RegisterDefaultSetting("Fonts", true)
    -- Modifies some default font objects.
    vCore:RegisterDefaultSetting("ObjectiveTracker", true)
    -- Adds player and cursor location to the world map.
    vCore:RegisterDefaultSetting("MapCoords", true)
    -- Makes the talking heads frame movable. /alertframemover to unlock the frame.
    vCore:RegisterDefaultSetting("MoveTalkingHeads", true)
    -- Add current number of quests to the worldmap title text.
    vCore:RegisterDefaultSetting("QuestTracker", true)
    -- Skin Omen, DBM, TinyDPS, Recount, Skada, and Numeration to closer match the Neav UI look.
    vCore:RegisterDefaultSetting("Skins", true)
    -- Add spell ids to the tooltip.
    vCore:RegisterDefaultSetting("SpellID", true)
    -- Alerts you when you detect a rare or chest.
    vCore:RegisterDefaultSetting("VignetteAlert", true)